// Components
export { FButton } from './FButton';
export type { FButtonProps } from './FButton';

// Import for re-export object
import { FButton } from './FButton';

// Re-export all components as a single object for easier importing
export const Components = {
  FButton,
};
